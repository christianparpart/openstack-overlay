# OpenStack Gentoo Portage Development Overlay

This overlay exists only for *developing* the OpenStack ebuilds, to be merged
into the main portage tree once the full stable stack is working as expected
for production use.
